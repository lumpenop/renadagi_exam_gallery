import React from 'react';
import {StyleSheet, View, Text} from 'react-native';

const FeedScreen = () => {
  return (
    <View>
      <Text>FeedScreen hi</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  block: {},
});

export default FeedScreen;
